PLUTO — BUILD LOG (see README.md for the full picture)
==================================================

Phase 1 — foundation
- FastAPI backend + vanilla-JS frontend (Vite); Vercel + Render deploys
- 12-tier cascade (Muse Spark / Nemotron 3.5 / DeepSeek V4 /
  Nemotron 3 Ultra / Big Pickle / MiMo V2.5 / Ling 3.0 / Groq /
  Gemini 3.6 / 3.5 Flash / OpenRouter x2)
- Custom tool loop (Gemini thought_signature fix), budgeted execution

Phase 2 — cost + reliability
- Deep Mode: planning + reflection; kind-based tier cooldowns
  (timeouts forgiven x2 strikes, quota 6h, auth 1h)
- Per-user budgets: LLM calls, tool calls, search calls, wall clock
- Real SSE streaming (live tokens, reset events, per-round status)

Phase 3 — quality + capabilities
- Structured web search + sources; read_pdf_page; vision fast-path
- csv_inspect (9 controlled ops); build_presentation; build_document
- Heuristic memory (regex + keyword scoring, no embeddings) with
  polarity/confidence

Phase 4 — RAG knowledge base
- Per-user vector store (JSON + cosine), Gemini embeddings
- Ingest on upload; search_documents tool; growth caps
  (docs + total chunks, kb-full past the cap)

Phase 5 — integrations + sandbox
- Gmail (search/read/draft/send) + Calendar tools, confirm-gated writes
- MCP client: allowlist-gated, minimal child env, output char-cap
- run_python: AST-validated sandbox, private mode only
- SQLite database tool (list/describe/query/import)

Phase 6 — workflows
- Owner-saved tool pipelines: templating, static-arg injection guards
  (code/sql/table/upload_id/server/tool + event_id/message_id/to/
  draft_id), blocked sinks (send_gmail), step caps

Phase 7 — accounts (current)
- Username/password signup/login/logout/me; PBKDF2 hashing (per-user
  salt, cleartext never stored), opaque Bearer session tokens
  (hashes only at rest), per-IP attempt limits
- Stable acct-<hex> ids isolate chats/memory/uploads/vectors per
  account in both auth modes; sidebar login dialog
- Logged-out browsers keep a stable visitor id (X-Pluto-Visitor), so
  open-mode chats persist instead of resetting every request

Fixes: IP-keyed rate limits (open mode), bounded executor queue (503),
auth unification, word-boundary router, history summary cache,
truthful tier attribution.

Repo: github.com/A-I-Mishel/poka (main)
Tests: pytest suite (mocked providers, zero quota spent)
