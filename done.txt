POKA — BUILD LOG (see README.md for the full picture)
==================================================

Phase 1 — cascade, tools, deploy, theme, memory, message actions
- 3→4-tier LLM cascade (Muse Spark 1.3 / Nemotron 3.5 / Gemini 3.6 / 3.5)
- Custom tool loop (Gemini thought_signature fix), runtime fallback
- Streamlit Cloud deploy (Python 3.12, Secrets, gitignored .env)
- Claude-style indigo dark UI; per-message Edit (truncate+prefill)
- Persistent chats + memory notes; hover Copy buttons
- Fixed: st.secrets breaking set_page_config; Enter rebinding; lost code

Phase 2 — cost + reliability
- Deep Mode toggle + conditional reflection (Fast default)
- Startup probe removed (lazy cascade); tier cooldown on first failure
- Structured tool statuses; friendly quota errors

Phase 3 — quality + capabilities (current)
- Structured web search (title/URL/snippet/date/domain) + Sources consulted
- read_pdf_page tool; scanned-PDF detection with honest OCR message
- Vision: owned-image analysis on Gemini tiers, honest fallback otherwise
- csv_inspect controlled ops (9 ops, no arbitrary code)
- build_presentation (5 layouts, overflow splits, notes, QC reopen check)
- build_document (markdown→structured DOCX, inline runs, QC reopen check)
- Semantic memory: types, polarity/negation, confidence, scoring,
  deletion API + UI control, DATA-delimited prompt injection
- ui/theme.py extraction; "Take a photo" wording; link-safe highlighting;
  UTC-internal timestamps; 7-day unreferenced-upload retention
- CI (compile+pytest blocking, pip-audit advisory), MIT LICENSE,
  README.md, pillow pinned

Repo: github.com/A-I-Mishel/poka (main). Live: Streamlit Cloud + localhost:8501.
Tests: 112 green (35 phase-1 + 40 phase-2 + 37 phase-3), zero quota spent.
