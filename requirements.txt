langchain>=1.3.9,<2
langchain-core>=1.3.3,<2
langchain-google-genai>=2.1.12,<3
langchain-openai>=1.1.14,<2
python-pptx>=1.0,<2
python-docx>=1.1,<2
pillow>=12.3.0,<14
pytesseract>=0.3.10,<1  # OCR wrapper (services/ocr.py); needs the tesseract system binary on PATH (soft dep: vision-OCR fallback covers its absence)
pandas>=2.0,<3
openpyxl>=3.1,<4
# Pinned transitive deps: scipy>=1.14 needs numpy>=2.0,<2.8;
# protobuf>=6.33.5 fixes PYSEC-2026-1805 (4.25.x vulnerable; fix in
# 5.29.6 / 6.33.5). The old google-ai-generativelanguage 0.6.x (<5 cap)
# is an orphan nothing imports — app uses langchain-google-genai's new
# google-genai SDK — so the <5 cap must not come back.
numpy>=2.0,<2.8
protobuf>=6.33.5,<7
pypdf>=6.16.1,<7
python-dotenv>=1.2.2,<2
fastapi>=0.115,<1
uvicorn>=0.30,<1
python-multipart>=0.0.18,<1
google-api-python-client>=2.150,<3
google-auth-oauthlib>=1.2,<2
boto3>=1.34,<2  # R2 data snapshots (services/snapshots.py); lazy import, optional at runtime
mcp>=1.0,<2
apscheduler>=3.10,<4  # background scheduler for storage hygiene
redis>=5.0,<6  # Redis rate limiter for distributed deployments
faiss-cpu>=1.15,<2  # ANN index for KB vector search
# Observability (services/obs.py + services/tracing.py + services/metrics.py).
# OTel SDK/API pinned together (1.44.0 tested); instrumentations 0.65b0 match
# that core. OTLP exporter is grpc (collector); prometheus_client backs /metrics.
opentelemetry-api>=1.44,<2
opentelemetry-sdk>=1.44,<2
opentelemetry-exporter-otlp>=1.44,<2
opentelemetry-instrumentation-fastapi>=0.65b0,<1
opentelemetry-instrumentation-httpx>=0.65b0,<1
opentelemetry-instrumentation-redis>=0.65b0,<1
opentelemetry-instrumentation-sqlite3>=0.65b0,<1
prometheus-client>=0.26,<1
python-json-logger>=3.0,<4  # structured JSON logs (services/structured_logging.py)
rapidfuzz>=3.0,<4  # typo-tolerant intent matching (services/normalize.py); difflib fallback if absent
# chromadb==0.5.5  # optional -- needs C++ build tools on Windows; app runs without it
